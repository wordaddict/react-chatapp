{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "heroku-postbuild": "cd chat && npm install && npm run build",
    "test": "mocha server/**/*.test.js",
    "test-watch": "nodemon --exec 'npm-test'"
  },
  "keywords": [
    "heroku"
  ],
  "engines": {
    "node": "10.8.0",
    "npm": "6.2.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.5",
    "moment": "^2.18.1",
    "socket.io": "^2.0.3"
  },
  "devDependencies": {
    "expect": "^1.20.2",
    "mocha": "^3.5.3",
    "nodemon": "^1.18.4"
  }
}
